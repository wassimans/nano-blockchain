{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "module": "ESNext",
    "outDir": "../../public",
    "inlineSources": true // Remove this line in production builds if revealing TS code to end users is problematic,
  }
}
